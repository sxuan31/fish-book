# Project Rules

- **强制必读**: 每次开启新的对话，或者被分配新任务时，必须优先读取并严格遵守本文件 (`.agent/rules.md`) 的所有规则。
- **语言规范**: 所有的对话和生成的文件（包括工件如 `task.md`、`implementation_plan.md` 等）都必须是中文的。必须使用中文与用户进行沟通。
- **回复风格**: 简洁、专业、高效。
